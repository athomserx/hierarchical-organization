import { EnvironmentInterface } from './envionment.interface';

export const environment: EnvironmentInterface = {
  environmentName: 'local',
  production: false,
  apiUrl: 'http://localhost:3000/api',
};
